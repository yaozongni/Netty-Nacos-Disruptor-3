一、Cassandra Timeline模型，核心消息与数据模型架构基础：Cassandra 使用了类似亚马逊 Dynamo 的分布式存储和复制机制，通过一致性哈希来分区数据。消息存储方式：它是“宽列”模型。这意味着虽然类似于关系型数据库，但它支持单行拥有极多列（万列级别），且每一行可以有不同的列，这对于即时消息（IM）、传感器数据等结构灵活的数据非常适用。高并发写入：Cassandra 采用 LSM（Log-Structured Merge-Tree）存储引擎，专门优化了高并发的写入场景。消息查询：为了获得高性能，数据建模必须以查询为导向，使用分区键（Partition Key）将数据分布在各个节点上。

现代IM系统（如微信、钉钉）的核心在于构建高并发、高可用、低延迟的消息收发与存储机制，通常基于Timeline（时间线）模型。实现上，采用“先存储后通知”策略，利用分布式存储（如Tablestore、HBase）记录消息，结合“推拉结合”模式实现多端同步与漫游。一、 核心架构与技术实现现代IM消息系统通常分为接入层、逻辑层、存储层：接入层（Access Layer）： 连接管理（WebSocket/TCP）、消息鉴权、协议解析。使用网关服务维持长连接。逻辑层（Logic Layer）： 负责单聊、群聊的路由、消息顺序生成、推拉逻辑。存储层（Storage Layer）： 基于Timeline模型，单聊（User Timeline）和群聊（Group Timeline）分开存储，实现高并发写入。二、 消息存储与同步实现 (基于Tablestore)利用表格存储的有序特性，实现消息高效有序存储和漫游。消息存储： 消息写入存储层后，产生唯一的单调递增消息ID（用于排序）。多端同步（推拉结合）：推（Push）： 在线时，服务器通过网关推送轻量级通知。拉（Pull）： 客户端接收到通知后，根据最新的Sequence ID从同步库增量拉取消息。消息漫游： 历史消息存储在服务端，用户更换设备后可自动拉取漫游历史。三、 关键问题解决消息时序： 使用分布式全局序列号生成器，保障消息写入时序与用户感知时序一致。读写扩散对比： 现代IM多采用写扩散（发送方写入一次，接收方拉取）以实现高可靠，[针对群聊]采用[读扩散]。消息状态： 实现“已读/未读”状态，通常基于用户对会话的最大已读ID记录。四、 技术选型协议： MQTT、WebSocket。消息队列： Kafka 或 RocketMQ，用于削峰填谷、处理离线消息。存储： TableStore (阿里云), HBase。

对同步库的查询会比较频繁，通常是对最新消息的查询，所以要求热数据尽量缓存在内存中，能提供高并发低延迟的查询。所以对同步库的配置，一般是需要SSD存储。

Tablestore Timeline的底层是基于LSM存储引擎的分布式数据库，LSM的最大优势就是对写入非常友好，天然适合消息写扩散的模式。同时对查询也做了极大优化，例如热数据进缓存、bloom filter等等。

同步库对于IM场景下的新消息即时感知统计这一核心功能，就是通过写入冗余的方式，提升新消息读取统计的效率与速度。对于IM场景没有收件箱的概念，因而同步库中冗余消息并没有永久保存的价值，提供7天过期时间已经足够保证功能正常。用户可以根据自身需求，调整同步库的数据过期时间（TTL）。
